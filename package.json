{
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && touch dist/.nojekyll",
    "deploy": "npx gh-pages -d dist -t true"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.0.0",
    "svelte": "^4.0.0",
    "vite": "^5.0.1"
  },
  "dependencies": {
    "sass": "^1.63.1"
  }
}